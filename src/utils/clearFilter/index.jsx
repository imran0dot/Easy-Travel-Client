const ClearFilter = () => {
    return (
        <p 
        // onClick={() => {  }} 
        className="underline text-secondary cursor-pointer">Clear filter</p>
    );
};

export default ClearFilter;